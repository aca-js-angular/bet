import { GameslengthsPipe } from './gameslengths.pipe';

describe('GameslengthsPipe', () => {
  it('create an instance', () => {
    const pipe = new GameslengthsPipe();
    expect(pipe).toBeTruthy();
  });
});
