import { SubcategoriesFilterPipe } from './subcategories-filter.pipe';

describe('SubcategoriesFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new SubcategoriesFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
