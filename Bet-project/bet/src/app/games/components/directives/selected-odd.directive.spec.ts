import { SelectedOddDirective } from './selected-odd.directive';

describe('SelectedOddDirective', () => {
  it('should create an instance', () => {
    const directive = new SelectedOddDirective();
    expect(directive).toBeTruthy();
  });
});
